# GetAWinner-Front - Víctor Aleman

Development of a Single-Page Application in React.js with TypeScript and Axios, using components, interfaces and Redux for state management.

The goal is to show the user each of the appropriate views of each use case (login, register, main...).

## How to run
1. Clone the repository.
2. Install the dependencies: `npm install`.
3. Start the development server (in the project directory): `npm start`.